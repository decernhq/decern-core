/**
 * Workspace CI token section – stub for self-hosted / open-source mode.
 * Real implementation lives in cloud/components/dashboard/workspace-ci-token-section.tsx.
 */
"use client";

export function WorkspaceCiTokenSection({
  workspaceId: _workspaceId,
  ciTokenCreatedAt: _ciTokenCreatedAt,
}: {
  workspaceId: string;
  ciTokenCreatedAt: string | null;
}) {
  return null;
}
