/**
 * Upgrade button – stub for self-hosted / open-source mode.
 * Real implementation lives in cloud/app/(dashboard)/dashboard/settings/upgrade-button.tsx.
 */
"use client";

export function UpgradeButton({
  planId: _planId,
  className: _className,
  size: _size,
}: {
  planId?: "team" | "business";
  className?: string;
  size?: "sm" | "md" | "lg";
}) {
  return null;
}
